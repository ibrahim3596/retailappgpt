# Android development configuration
# Do not place production secrets in this file.
# These are non-secret placeholders so BuildConfig remains valid in CI.
GEMINI_API_KEY=CONFIGURE_ME
SUPABASE_URL=https://placeholder.invalid
SUPABASE_ANON_KEY=CONFIGURE_ME
